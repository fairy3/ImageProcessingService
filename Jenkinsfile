pipeline {
    agent any
   
    triggers {
      githubPush()
    }
	    
    stages {
        stage('Build') {
            steps {
                echo 'push has happened'
            }
        }
    }
}
