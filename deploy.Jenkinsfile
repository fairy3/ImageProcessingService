pipeline {
    agent any
	   
    parameters {
         string(name: 'IMAGE_URL', defaultValue: '', description: '')
    }

    stages {
        stage('Deploy') {
            steps {
                sh '''
                  # deploying to k8s cluster...


                    '''
            }
        }
    }
}
